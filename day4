#include <stdio.h>
#include <string.h>
int main() {
    // Write C code here
    char arr[30];
    fgets(arr,sizeof(arr),stdin);
    puts(arr);
    int len=strlen(arr);
    printf("%d",len-1);

    return 0;
}
